# Deployment directory

`core-ces-log-parse` is a local packaged CLI and currently has no service,
container, or infrastructure deployment manifests. The package build and
installed-wheel smoke test are defined in the [CI workflow](../.github/workflows/ci.yml).

This scoped index records the current boundary. Add deployment artifacts only
when a supported deployment target exists.
