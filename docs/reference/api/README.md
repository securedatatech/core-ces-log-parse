# API reference

This project currently exposes a CLI rather than a documented library API.
The package initializer is intentionally empty, and helper functions in
[`main.py`](../../../src/core_ces_log_parse/main.py) support the CLI's
implementation and tests.

Treat the CLI and its documented file formats as the supported boundary. Add
API reference material only when a stable import surface is intentionally
committed.
