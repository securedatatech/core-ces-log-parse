# Architecture decision records

No architecture decision records currently exist. This index is the landing
page for future durable decisions; it does not create a decision or replace
the current source and test documentation.

Use the root [`CONTEXT.md`](../../CONTEXT.md) for domain vocabulary and the
[developer architecture index](../developers/architecture/README.md) for the
current implementation boundary. Existing agent guidance describes ADR
documents as lazy; the required index is now present while the decision
record collection remains intentionally empty.
